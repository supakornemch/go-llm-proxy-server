# 🇹🇭 คู่มือการใช้งาน Go LLM Proxy Server

คู่มือนี้จะช่วยให้คุณเริ่มใช้งาน LLM Proxy Server ได้อย่างรวดเร็ว พร้อมตัวอย่างการตั้งค่าสำหรับ Provider ยอดนิยม

---

## 1. บทนำ (Overview)

Go LLM Proxy Server คือระบบตัวกลาง (Proxy) ที่ช่วยให้คุณบริหารจัดการ API Key ของ LLM หลายค่าย (OpenAI, Gemini, Azure, Bedrock) ได้จากที่เดียว โดยคุณสามารถสร้าง **Virtual Keys** ให้ทีมงานใช้งานแทน Key จริง ซึ่งช่วยเรื่องความปลอดภัยและการจำกัดโควต้า (Rate Limiting)

### แนวคิดหลัก (Key Concepts)
1.  **Connection**: การเชื่อมต่อไปยัง LLM Provider จริงๆ (เช่น OpenAI, Google)
2.  **Provider Model**: การระบุ Model ภายใต้ Connection นั้นๆ (เช่น gpt-4o, gemini-1.5-flash)
3.  **Virtual Key**: คีย์จำลองที่คุณสร้างขึ้นมาเพื่อให้ Client ใช้งาน
4.  **Assignment**: การกำหนดสิทธิ์ว่า Virtual Key ไหน ใช้ Model อะไรได้บ้าง พร้อมกำหนด TPS และ Token Limit

---

## 2. การติดตั้ง (Setup)

### ผ่าน Docker Compose (แนะนำ)
1. เตรียมไฟล์ `.env`
2. รันคำสั่ง:
   ```bash
   docker compose up --build -d
   ```

### การตั้งค่า Admin (Master Key)
คุณสามารถกำหนด `MASTER_KEY` ใน Environment Variable เพื่อเข้าใช้งานทุก Model ได้ทันทีโดยไม่ต้องตั้งค่า Assignment:
```bash
export MASTER_KEY="my-admin-key"
```

---

## 3. การใช้งาน CLI (CLI Usage)

### ขั้นตอนที่ 1: เพิ่ม Connection
```bash
./llm-proxy connection add --name "google-ai" --provider "google" --endpoint "https://generativelanguage.googleapis.com/v1beta/openai" --api-key "AIza..."
```

### ขั้นตอนที่ 2: เพิ่ม Model
```bash
./llm-proxy model add --name "gemini-flash" --remote "gemini-1.5-flash" --conn-id "<CONN_ID>"
```

### ขั้นตอนที่ 3: สร้าง Virtual Key พร้อมกำหนดสิทธิ์ (New!)

**แบบระบุ Connection (ใช้ได้ทุก model ในค่ายนั้น):**
```bash
./llm-proxy vkey add --name "Mobile-App" --key "sk-proxy-123" --conn-id "<CONN_ID>"
```

**แบบระบุราย Model (จำกัดสิทธิ์สูงสุด):**
```bash
./llm-proxy vkey add --name "Tester" --key "sk-tester-456" --model-id "<MODEL_ID>"
```

---

## 4. ตัวอย่างการเรียกใช้งาน (Examples)

### Python (LangChain)
คุณสามารถใช้ `ChatOpenAI` เรียกใช้งาน Gemini ผ่าน Proxy ได้ทันที:

```python
from langchain_openai import ChatOpenAI

llm = ChatOpenAI(
    model="gemini-flash",           # ชื่อ alias ที่ตั้งไว้ใน Proxy
    api_key="sk-proxy-123",         # Virtual Key ที่สร้างขึ้น
    base_url="http://localhost:8080/v1"
)

print(llm.invoke("สวัสดี!").content)
```

### cURL (AWS Bedrock / Claude)
```bash
curl http://localhost:8080/v1/chat/completions \
  -H "Authorization: Bearer sk-proxy-123" \
  -d '{
    "model": "claude-3-sonnet",
    "messages": [{"role": "user", "content": "Hello Bedrock!"}]
  }'
```

---

## 5. การจัดการฐานข้อมูล

ระบบรองรับ:
- **SQLite**: เหมาะสำหรับการทดสอบ (ระบุ `DB_TYPE=sqlite`)
- **PostgreSQL**: เหมาะสำหรับ Production (ระบุ `DB_TYPE=postgres`)
- **MongoDB**: สำหรับสาย NoSQL (ระบุ `DB_TYPE=mongodb`)

---

สร้างด้วย ❤️ โดยทีมพัฒนา Go LLM Proxy
