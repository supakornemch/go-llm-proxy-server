#!/bin/bash

# =================================================================================================
# LLM Proxy Server - Smoke Test Example
# =================================================================================================
# Copy this file to 'smoke_test.sh' and fill in your private configurations.
# 'smoke_test.sh' is ignored by Git to prevent leaking provider info or keys.
# =================================================================================================

PORT=8132
VKEY="sk-smoke-test-$(date +%s)"
DB_TYPE=mongodb
DB_DSN="mongodb://root:examplepassword@localhost:27017/llm_proxy?authSource=admin"

# Seed your connections here using the CLI
# Example:
# OPENAI_ID=$(./llm-proxy connection add --provider "openai" --endpoint "$OPENAI_ENDPOINT" --api-key "$OPENAI_KEY" ...)

echo "Run 'bash scripts/smoke_test.sh' to start testing."
